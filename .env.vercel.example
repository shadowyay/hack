# Vercel Environment Variables Template
# Copy these to your Vercel Project Settings → Environment Variables

# Backend API URL (REQUIRED)
VITE_BACKEND_URL=https://your-backend-name.onrender.com

# AI Service URL (REQUIRED)
VITE_AI_SERVICE_URL=https://your-ai-service-name.onrender.com

# Environment (REQUIRED)
VITE_APP_ENV=production

# Optional: WebSocket URL (if different from backend)
# VITE_WEBSOCKET_URL=wss://your-backend-name.onrender.com

# Optional: MongoDB URI (if frontend needs direct access)
# VITE_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bounty_hunters_db

# Optional: API Keys (if needed by frontend)
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# VITE_SENTRY_DSN=https://your-sentry-dsn

# Note: Replace 'your-backend-name' and 'your-ai-service-name' with actual service names from Render
